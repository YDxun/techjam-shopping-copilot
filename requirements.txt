# TechJam2026 Shopping Copilot Agent dependency list (offline + online)
#
# Python >= 3.10; the core offline mode needs only the Python standard library + sqlite3 FTS5.
# Everything below is an OPTIONAL enhancement: when a package is missing, the agent auto-degrades
# (capability_probe detects it -> rule fallback). The default runs fully offline with zero API.

# ---- Optional LLM client (online rerank / intent; needs key + network) ----
# openai>=1.0,<2.0          # OpenAI SDK: unified DeepSeek/OpenAI client + qwen3-rerank MaaS
# httpx>=0.24               # network probe / retry helper

# ---- Optional dense retrieval (BLaIR, CPU-capable) ----
# transformers>=4.40        # BLaIR hyp1231/blair-roberta-large query encoding (AutoModel CLS)
# torch>=2.0                # CPU/CUDA tensors; also needed by scripts/encode_catalog_blair.py offline encoding
# numpy>=1.24

# ---- Optional cross-encoder rerank models ----
# FlagEmbedding>=1.3        # BAAI/bge-reranker-v2-m3
# huggingface-hub>=0.20     # model download / local-cache detection
# e-commerce generative rerankers RexReranker-0.6B / Qwen3-Reranker reuse transformers + torch

# ---- Optional standalone retrieval pipeline (retrieval_pipeline/) ----
# rank-bm25>=0.2.2
# pydantic>=2.0


# ---- Dev/quality tools (formerly requirements-dev.txt, unified into this file) ----
ruff==0.15.1                # linter (pyproject.toml's [tool.ruff] config targets this version)
