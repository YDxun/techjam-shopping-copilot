# Default application profile: official Agent entry, BM25 retrieval, and
# optional DeepSeek/OpenAI-compatible calls. Python >= 3.10 is required.

# Imported by utils.blair, which is part of the main Agent import chain.
numpy>=1.24

# Direct dependency of the shared OpenAI-compatible client.
httpx>=0.25,<1.0
openai>=1.0,<2.0
