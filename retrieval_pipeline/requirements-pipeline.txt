# 检索管线（赛题第4-6步）依赖
# 核心（必装）：pydantic + numpy + rank-bm25（纯 Python，无 GPU 要求）
pydantic>=2.0
numpy>=1.24
rank-bm25>=0.2.2
torch>=2.0

# 可选增强（未安装自动降级，不影响离线运行）：
#   第5步-通道3 稠密查询编码（BLaIR，推荐）：
#     transformers>=4.40    # hyp1231/blair-roberta-large 查询编码（AutoModel CLS）
#     sentence-transformers>=2.2   # 兜底编码器（部分环境）
#   商品离线向量化（一次性预处理，产物 data/offline_blair_embeds.npy）：
#     python scripts/encode_catalog_blair.py
#   第6步重排（bge-reranker-v2-m3）：
#     FlagEmbedding>=1.3     # BAAI/bge-reranker-v2-m3（本地缓存可离线）
#   第4步 LLM 查询改写（QUERY_REWRITE_ENABLE=true）：openai>=1.0
